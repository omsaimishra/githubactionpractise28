name: hello action
on: 
 push: 
   branches:
    - main 
 pull_request:
 schedule: 
  - cron: "0 0 * * *"

jobs:
   helloaction:
     runs-on: ubuntu-latest
     steps:
      - name: hello action
        run: echo "hello-action"
   envandsecrets:
     runs-on: ubuntu-latest
     needs: helloaction
     env:
       name: chiku
     steps:
       - run: echo "$name"
       - run: echo ${{secrets.MAIN_SO}}
      
        
    
